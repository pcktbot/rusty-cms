# UI

Planned stack:

- Bun
- TypeScript
- SvelteKit

This directory is intentionally light until the headless API, page document model, and publish workflow contracts settle.

For now, a temporary browser surface is served directly by the Rust API at `/viewer` so render work can be exercised before the dedicated UI app is built.
